# 匹配
- Gale-Shapely算法算一个稳定匹配
- 名词解释完美匹配和稳定匹配
# 效率
- 给一个多循环的程序，算$O(f(n))$
- 算$\Theta(f(n))$
- 给一个别的算法，让其复杂度$O(g(n))$满足$\lim_{ n \to \infty }\frac{g(n)}{f(n)}=0$
# 图
用DFS算法找有向图当中的圈并计算复杂度
# 贪心算法
要求复杂度为$O(m+n)$ 
在序列$S=(s_{1},s_{2},\dots s_{n})$当中找$P=(p_{1},p_{2},\dots,p_{m})$,中间可以有间隔
- 给出思想
- 伪代码
- 复杂度分析
# 分治
给定连续 n 天股票价格：$p[1],p[2],\dots,p[n]$
先买后卖，要求最大利润
设计一个分治算法
分析复杂度
# 动态规划
给两个大写字母序列，算最大共同序列及其长度
- 给出长度的状态方程
- 分别给出二者伪代码和复杂度分析
# 网络流
- 名词解释：剩余图，增广路径
- 解释FF算法流程
- 证明FF算法返回最大流（先证明最大流=最小割，在证明FF返回最大流）
# NP
- 名词解释：多项式归约
- 给出3-SAT和k顶点覆盖的规约方法
