# 使用指针交换数组中相邻元素的位置
## 【问题描述】

    编写函数 void swapAdjacent(int* arr, int size) 实现数组中相邻元素位置交换（如数组中元素个数为奇数，则不对最后一个元素进行操作）。该函数接收两个输入，其中 int* arr 为待操作的数组，int size 为数组中元素的个数

## 【输入形式】

    首先输入数组元素个数，随后依次输入数组的每个元素

## 【输出形式】

    输出操作后的数组（依次输出操作后的数组的每个元素，每个元素后紧接一个空格）

## 【样例输入】

    请输入数组的元素个数: 5

    请输入数组的元素: 7 9 4 6 2

## 【样例输出】

    交换相邻元素后的数组: 9 7 6 4 2 

## 【样例说明】

    输入时只输入 “5” 和 “7 9 4 6 2”，文字部分用作题目说明

    输出时只输出 “9 7 6 4 2 ”，文字部分用作题目说明
# 使用指针交换数组中最大和最小元素的位置
## 【问题描述】

    编写函数 void swapMaxMin(int* arr, int size) 实现数组中最大元素和最小元素位置交换（有多个最大或最小值时，仅对最先出现的最小值和最先出现的最大值进行操作）。该函数接收两个输入，其中 int* arr 为待操作的数组，int size 为数组中元素的个数【输入形式】

    首先输入数组元素个数，随后依次输入数组的每个元素

## 【输出形式】

    输出操作后的数组（依次输出操作后的数组的每个元素，每个元素后紧接一个空格）

## 【样例输入】

    请输入数组的元素个数: 5

    请输入数组的元素: -5 0 8 10 3

## 【样例输出】

    交换最大值和最小值位置后的数组: 10 0 8 -5 3 

## 【样例说明】

    输入时只输入 “5” 和 “-5 0 8 10 3”，文字部分用作题目说明

    输出时只输出 “10 0 8 -5 3 ”，文字部分用作题目说明
按顺序打印单链表中的元素
题目要求：

    请在以下代码中补全 void printList(ListNode* head) 函数，以实现按顺序打印链表的元素。

示例输入：

    请输入整数，输入 -1 结束输入：

    1 2 3 4 5 -1

实例输出：

    1 2 3 4 5 

输出要求：

    每个数字后均包含一个空格，若链表中不含元素，则不输出任何内容

题目代码：
```cpp

# include <iostream>



// 定义链表节点结构体

struct ListNode {

    int val;

    ListNode* next;

    ListNode(int x) : val(x), next(nullptr) {}

};



// 根据输入构建链表

ListNode* buildList() {

    ListNode dummy(0);

    ListNode* tail = &dummy;

    int num;

    while (std::cin >> num && num != -1) {

        tail->next = new ListNode(num);

        tail = tail->next;

    }

    return dummy.next;

}



// 打印链表的函数

void printList(ListNode* head) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能

}



int main() {

    std::cout << "请输入整数，输入 -1 结束输入：" << std::endl;

    ListNode* head = buildList();



    // 调用你编写的打印链表函数

    // printList(head); 



    return 0;

}
```
删除单链表中的指定元素
题目要求：

    在上一题的基础上，请在以下代码中补全 ListNode* removeElements(ListNode* head, int val) 函数，以实现按顺序打印链表的元素。

    该函数返回删除指定元素 val 后的链表的表头指针。

示例输入输出：

    [输出]请输入整数，输入 -1 结束输入：

    [输入]1 2 3 4 5 -1

    [输出]原始链表：

    [输出]1 2 3 4 5 

    [输出]请输入要删除的整数值：

    [输入]2

    [输出]删除指定值节点后的链表：

    [输出]1 3 4 5 

输出要求：

    每个数字后均包含一个空格，若链表中不含元素，则不输出任何内容

题目代码：


#include <iostream>



// 定义链表节点结构体

struct ListNode {

    int val;

    ListNode* next;

    ListNode(int x) : val(x), next(nullptr) {}

};



// 根据输入构建链表

ListNode* buildList() {

    ListNode dummy(0);

    ListNode* tail = &dummy;

    int num;

    while (std::cin >> num && num != -1) {

        tail->next = new ListNode(num);

        tail = tail->next;

    }

    return dummy.next;

}



// 打印链表的函数

void printList(ListNode* head) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能



// 删除指定值的节点

ListNode* removeElements(ListNode* head, int val) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能

}



int main() {

    std::cout << "请输入整数，输入 -1 结束输入：" << std::endl;

    ListNode* head = buildList();



    std::cout << "原始链表：" << std::endl;

    // printList(head);



    int target;

    std::cout << "请输入要删除的整数值：" << std::endl;

    std::cin >> target;



    // ListNode* newHead = removeElements(head, target);



    std::cout << "删除指定值节点后的链表：" << std::endl;

    // printList(newHead);



    return 0;

}

删除单链表中的指定元素
题目要求：

    在上一题的基础上，请在以下代码中补全 ListNode* removeElements(ListNode* head, int val) 函数，以实现按顺序打印链表的元素。

    该函数返回删除指定元素 val 后的链表的表头指针。

示例输入输出：

    [输出]请输入整数，输入 -1 结束输入：

    [输入]1 2 3 4 5 -1

    [输出]原始链表：

    [输出]1 2 3 4 5 

    [输出]请输入要删除的整数值：

    [输入]2

    [输出]删除指定值节点后的链表：

    [输出]1 3 4 5 

输出要求：

    每个数字后均包含一个空格，若链表中不含元素，则不输出任何内容

题目代码：


#include <iostream>



// 定义链表节点结构体

struct ListNode {

    int val;

    ListNode* next;

    ListNode(int x) : val(x), next(nullptr) {}

};



// 根据输入构建链表

ListNode* buildList() {

    ListNode dummy(0);

    ListNode* tail = &dummy;

    int num;

    while (std::cin >> num && num != -1) {

        tail->next = new ListNode(num);

        tail = tail->next;

    }

    return dummy.next;

}



// 打印链表的函数

void printList(ListNode* head) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能



// 删除指定值的节点

ListNode* removeElements(ListNode* head, int val) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能

}



int main() {

    std::cout << "请输入整数，输入 -1 结束输入：" << std::endl;

    ListNode* head = buildList();



    std::cout << "原始链表：" << std::endl;

    // printList(head);



    int target;

    std::cout << "请输入要删除的整数值：" << std::endl;

    std::cin >> target;



    // ListNode* newHead = removeElements(head, target);



    std::cout << "删除指定值节点后的链表：" << std::endl;

    // printList(newHead);



    return 0;

}

删除单链表中的指定元素
题目要求：

    在上一题的基础上，请在以下代码中补全 ListNode* removeElements(ListNode* head, int val) 函数，以实现按顺序打印链表的元素。

    该函数返回删除指定元素 val 后的链表的表头指针。

示例输入输出：

    [输出]请输入整数，输入 -1 结束输入：

    [输入]1 2 3 4 5 -1

    [输出]原始链表：

    [输出]1 2 3 4 5 

    [输出]请输入要删除的整数值：

    [输入]2

    [输出]删除指定值节点后的链表：

    [输出]1 3 4 5 

输出要求：

    每个数字后均包含一个空格，若链表中不含元素，则不输出任何内容

题目代码：


#include <iostream>



// 定义链表节点结构体

struct ListNode {

    int val;

    ListNode* next;

    ListNode(int x) : val(x), next(nullptr) {}

};



// 根据输入构建链表

ListNode* buildList() {

    ListNode dummy(0);

    ListNode* tail = &dummy;

    int num;

    while (std::cin >> num && num != -1) {

        tail->next = new ListNode(num);

        tail = tail->next;

    }

    return dummy.next;

}



// 打印链表的函数

void printList(ListNode* head) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能



// 删除指定值的节点

ListNode* removeElements(ListNode* head, int val) {

    // 请在此处补全该函数，并在 main() 函数中调用以实现其功能

}



int main() {

    std::cout << "请输入整数，输入 -1 结束输入：" << std::endl;

    ListNode* head = buildList();



    std::cout << "原始链表：" << std::endl;

    // printList(head);



    int target;

    std::cout << "请输入要删除的整数值：" << std::endl;

    std::cin >> target;



    // ListNode* newHead = removeElements(head, target);



    std::cout << "删除指定值节点后的链表：" << std::endl;

    // printList(newHead);



    return 0;

}
