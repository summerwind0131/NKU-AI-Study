# 选择涉及考点：
- 椒盐噪声用那种滤波处理最有效
- 8位灰度级范围
- 棋盘距离计算：（2，3），（6，6）
- HOG的目标
- OSTU的目标
- ResNet的特点和对于F(x)+x的处理
- 采样
- 计算卷积层输出图像大小
- CNN各个层的作用
# 判断：
- 机器视觉是个逆过程，所以更难(T/F)
- Hough变换的基本原理
- 亮度变换仅仅是增加亮度，不涉及对比度和减小亮度(T/F)
- 验证集和测试集都可以用来迭代CNN（T/F）
- 忘了
# 简答题:
- 滤波图像处理的分类和特点
- canny边缘检测比sobel效果更好的原因
- 计算卷积层参数数量
- 解释epoch, batch size, iteration，并计算2000训练参数，bactch size=16, 一个epoch当中的迭代次数
- ROI Pooling和一般池化的区别
# 解答题：
- sift特征检测：
    1）解释尺度不变性的原理 
    2）为什么生成特征描述子以及特征描述子的作用
    3）如何进行特征匹配
- 给了个矩阵和结构元素，给出开运算和闭运算结构，并比较两种运算作用
    6乘7的矩阵
    结构元素：$$\begin{bmatrix}
		1 & 1 \\
		\mathbb{1}&1
	\end{bmatrix}$$
# 编程题：
- Sobel边缘检测：实现replicate的imfilter（可以调用conv2(f,h)）+实现Sobel边缘检测
- ResNet : Basic Block当中init和forward函数的填空